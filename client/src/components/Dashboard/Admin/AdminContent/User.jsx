import React from 'react';


export default function User({ pagination, onNext, onPrev }) {
    return (
      <div className="pagination-controls">
            hello i am the user
      </div>
    );
  }